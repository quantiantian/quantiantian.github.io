define([
        'jquery',
        'angular',
        'app',
        'scripts/controller/myController'
    ],
    function(jquery, angular, app) {
        angular.bootstrap(document, ['myApp']);
    }
);
